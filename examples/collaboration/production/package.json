{
  "name": "collaboration-in-production-example",
  "version": "1.0.0",
  "private": true,
  "description": "Minimal collaboration example with SuperDoc and Y.js",
  "scripts": {
    "dev": "concurrently \"pnpm run dev --prefix server\" \"pnpm run dev --prefix client\" --names \"server,client\" --prefix-colors \"blue,green\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
